import mysql.connector

con = mysql.connector.connect(host='localhost', user='lostboyar',password="Lost@567",database='bca5thcd')

mycursor=con.cursor()

mycursor.execute("select * from student1")
for row in mycursor:
    print(row)